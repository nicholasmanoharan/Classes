#pragma once
#include "Media.h"

class Movie : public Media {

public: 
    movie (const std::string& title, int year, const std::stirng& director, int duration);
    void displayInfo() const override;
    bool search(const std::string& term) const override;
    int getRating() const;

private:
    std::string director;
    int duration;
    int rating;

};
